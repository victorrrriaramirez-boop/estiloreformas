"use client";
import { motion, useScroll, useSpring, useTransform } from "framer-motion";
import { useRef } from "react";
export default function BeforeAfterStory(){const ref=useRef<HTMLElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:["start start","end end"]});const p=useSpring(scrollYProgress,{stiffness:70,damping:28});const clip=useTransform(p,[0,.9],["inset(0 88% 0 0)","inset(0 8% 0 0)"]);const x=useTransform(p,[0,.9],["12%","88%"]);const op1=useTransform(p,[0,.18,.34],[1,1,.18]);const op2=useTransform(p,[.28,.5,.66],[.18,1,.18]);const op3=useTransform(p,[.6,.78,1],[.18,1,1]);return <section ref={ref} className="ba-wrap"><div className="ba-sticky shell"><div className="ba-head"><p className="eyebrow">SCROLL 02 · ANTES / DESPUÉS</p><h2>El cambio no es decorar. Es reorganizar.</h2></div><div className="ba-scene"><div className="ba-room before"><span>ANTES</span><div className="old-door"/><div className="old-cabinet"/><div className="old-table"/></div><motion.div className="ba-room after" style={{clipPath:clip}}><span>DESPUÉS</span><div className="new-window"/><div className="new-island"/><div className="new-light"/></motion.div><motion.div className="ba-handle" style={{left:x}}><i/></motion.div></div><div className="ba-notes"><motion.p style={{opacity:op1}}><b>01 · LUZ</b> Abrir visuales y eliminar barreras permite que la luz llegue más lejos.</motion.p><motion.p style={{opacity:op2}}><b>02 · CIRCULACIÓN</b> Menos pasillos y mejores recorridos convierten metros perdidos en espacio útil.</motion.p><motion.p style={{opacity:op3}}><b>03 · CONTINUIDAD</b> Una paleta coherente hace que cocina, salón y zonas de paso pertenezcan a la misma casa.</motion.p></div></div></section>}
