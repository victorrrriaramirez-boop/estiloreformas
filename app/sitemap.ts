import type { MetadataRoute } from "next";import {site} from "@/lib/site";export default function sitemap():MetadataRoute.Sitemap{const now=new Date();return [{url:site.url,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${site.url}/contacto`,lastModified:now,changeFrequency:"monthly",priority:.6},...site.services.map(s=>({url:`${site.url}/servicios/${s.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.8}))]}
